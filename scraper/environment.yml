name: oscabet-scraper
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pandas
  - numpy
  - pip
  - pip:
      - requests
      - cloudscraper
      - tqdm
      - python-dotenv
